// Enums for startup data
export enum StartupStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum StartupStage {
  IDEA = 'idea',
  PROTOTYPE = 'prototype',
  MVP = 'mvp',
  GROWTH = 'growth',
  SCALE = 'scale',
}

export enum FounderGender {
  MALE = 'male',
  FEMALE = 'female',
  NON_BINARY = 'non-binary',
  MIXED = 'mixed',
  PREFER_NOT_TO_SAY = 'prefer-not-to-say',
}
